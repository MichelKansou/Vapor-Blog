#extend("Layouts/admin")

#export("head") {
	<title>My App</title>
}

#export("body") {
    <div class="list-container">
        #if(user) {
            <h1>Hello #(user.username) :D</h1>
        }
    </div>
}
