<!DOCTYPE html>
<html>
<head>
    #import("head")
    <link href="https://fonts.googleapis.com/css?family=Quicksand:400,700,300" rel="stylesheet">
    <link rel="stylesheet" href="/styles/app.css">
    <link rel="stylesheet" href="/styles/font-awesome.min.css">
</head>
    <body>
        <header class="admin">
            <nav>
                <ul>
                    <li><a href="/" class="nav-item #if(homePage) {active}">Home</a></li>
                    <li><a href="posts #if(postPage) {active}">Posts</a></li>
                </ul>
            </nav>

            <div class="user-menu">
                <ul>
                    <li><a href="/admin/my-profile" class="nav-item #if(homePage) {active}">My profile</a></li>
                    <li>
                        <form action="/logout" method="POST">
                            <button class="btn logout" type="submit">logout</button>
                        </form>
                    </li>
                </ul>
            </div>
        </header>
        <main>
            #import("body")
        </main>
    </body>
</html>
